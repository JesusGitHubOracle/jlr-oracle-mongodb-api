# Kafka Connect console sink.
# Prints records from mongo.orders.cdc so you can verify the source connector output.

name=console-sink-orders
connector.class=org.apache.kafka.connect.file.FileStreamSinkConnector
tasks.max=1

topics=mongo.orders.cdc
file=/dev/stdout
